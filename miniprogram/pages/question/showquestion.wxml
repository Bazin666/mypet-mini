<view>
    <mp-cells title="我的问题：">
        <mp-cell link hover wx:for="{{question_l}}" wx:for-item="q" wx:for-index="i" wx:key="id" value="{{q.title}}" data-item="{{q}}" data-qid="{{q}}" footer="问题{{i}}：" bindtap="select_q"></mp-cell>
    </mp-cells>
</view>